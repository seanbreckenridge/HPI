#!/bin/bash
# remind me to log my weight

evry 30 minutes -logweight && {
	cd "$(realpath "$(dirname "${BASH_SOURCE[0]}")")"
	printlog "check_weight:checking..."
	./check_weight.py || notify-send "havent updated weight in more than a week!"
}
